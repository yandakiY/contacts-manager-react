import { Button, Grid, Typography } from '@mui/material';
import React from 'react';
import { makeStyles } from '@mui/styles';
const useStyles = makeStyles(() => ({
  
}))

const testComponent = () => {
//   const classes = useStyles();

  return (
    // <div>
      {/* <h1 className={classes.title1} >Hello world</h1>
      <h1 className={classes.title2} >Hello world</h1>
      <h1 className={classes.title3} >Hello world</h1>

      <Grid container spacing={4}>
        <Grid item xs={12}>
          <Button variant='outlined'>
            Send a File
          </Button>
        </Grid>

        <Grid style={{textAlign:'center' , border:'solid 1px purple'}} item xs={12}>
          <Typography className={classes.root}>
            First bar
          </Typography>
        </Grid>

        <Grid style={{textAlign:'center' , border:'solid 1px purple'}} item xs={6}>
          <Typography>
            feed
          </Typography>
        </Grid>

        <Grid style={{textAlign:'center' , border:'solid 1px purple'}} item xs={6}>
          <Typography>
            right bar
          </Typography>
        </Grid>
      </Grid>
    </div> */}
  );

}

export default testComponent;
